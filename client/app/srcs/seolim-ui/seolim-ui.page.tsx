import React from 'react';
import {Input} from '@seolim/designsystem';

const SeolimUiPage: React.FC = () => (
  <div>
    <h1>Seolim UI Page</h1>
    <Input placeholder="Type something..." />
  </div>
);

export default SeolimUiPage;
