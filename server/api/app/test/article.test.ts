import { getArticle } from '../controller';
import { Article } from '../model';

describe('Article Test', () => {

  test('Get Articles', async () => {
    // Article.get = jest.fn().mockReturnValue({ path: 'test/test.md' });
    // const title = 'test.md';
    // const category_id = 'ididi';
    // const user_email = 'test';
    // const result = await getArticle(title, category_id, user_email);
    // expect(result).toMatch('asd')
  })
});
