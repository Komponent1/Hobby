export enum CharactorStatus {
  IDLE = 'IDLE',
  ATTACK = 'ATTACK',
  DEAD = 'DEAD',
}
export enum MoveDirection {
  UP = -1,
  DOWN = 1,
  LEFT = -1,
  RIGHT = 1,
}
export enum BulletStatus {
  LOADED = 'LOADED',
  SHOOTED = 'SHOOTED',
}
